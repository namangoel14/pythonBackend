# 1. Create a class (2-D vector) and use it to create another class representing a 3-D vector.

class TwoDVector:

    def __init__(self, i, j):
        self.i = i
        self.j = j
    def show(self):
        print(f"2D vector is {self.i}i + {self.j}j")

class ThreeDVector(TwoDVector):
    def __init__(self, i, j, k):
        super().__init__(i, j)
        self.k = k
    def show(self):
        print(f"3D vector is {self.i}i + {self.j}j + {self.k}k")

m = TwoDVector(1, 2)
n = ThreeDVector(1, 3, 4)

m.show()
n.show()

# 2. Create a class ‘Pets’ from a class ‘Animals’ and further create a class ‘Dog’ from ‘Pets’. Add a method ‘bark’ to class ‘Dog’.

class Animals:
    def __init__(self, animals):
        self.animals = animals

class Pets(Animals):
    def __init__(self, animals, pets):
        super().__init__(animals)
        self.pets = pets
    def pet(self):
        if self.animals == self.pets:
            print(f"I have a pet {self.pets}")

class Dog(Pets):
    def __init__(self, animals, pets, speaks):
        super().__init__(animals, pets)
        self.speaks = speaks
    def bark(self):
        print(f"My {self.pets} {self.speaks} a lot on strangers")

p = Dog("dog", "dog", "barks")
p.pet()
p.bark()

# 3. Create a class ‘Employee’ and add salary and increment properties to it. Write a method ‘salaryAfterIncrement’ method with a @property decorator with a setter which changes the value of increment based on the salary.

"""
class Employee:
    def __init__(self, salary, increment):
        self._salary = int(salary)
        self._increment = float(increment)

    @property
    def salaryAfterIncrement(self):
        if self._salary >= 350000 and self._salary <= 500000:
            self._increment = self._salary * self._increment
            self._salary = self._salary + self._increment
        elif self._salary > 500000 and self._salary <= 1000000: 
            self._increment = self._salary * self._increment
            self._salary = self._salary + self._increment
        elif self._salary > 1000000 and self._salary <= 2000000:
            self._increment = self._salary * self._increment
            self._salary = self._salary + self._increment

        return self._salary

l = Employee(559000, 0.15)

print("Your incremented salary:",l.salaryAfterIncrement)
"""

class Employee:
    
    def __init__(self, salary, increment):
        self.salary = salary
        self.increment = increment

    @property
    def salaryAfterIncrement(self):
        return self.salary * (1 + self.increment)
    @salaryAfterIncrement.setter
    def salaryAfterIncrement(self, salary):
        self.increment = ((salary/self.salary) - 1) 

l = Employee(500000, 0.18)
print(f"Incremented salary is {l.salaryAfterIncrement}")
print(f"Incremented percentage is {l.increment * 100}%")


# 4. Write a class ‘Complex’ to represent complex numbers, along with overloaded operators ‘+’ and ‘*’ which adds and multiplies them.

class Complex:
    
    def __init__(self, real, img):
        self.real = real
        self.img = img
    def __add__(self, other):
        real_parts = self.real + other.real
        img_parts = self.img + other.img

        return Complex(real_parts, img_parts)

    def __mul__(self, other):
        real_parts = self.real * other.real
        img_parts = self.img * other.img

        return Complex(real_parts, img_parts)

    def __str__(self):
        return f"{self.real} + {self.img}j"


complex1 = Complex(3, 4)
complex2 = Complex(5, 6)

result_add = complex1 + complex2
result_mul = complex1 * complex2
print(result_add)
print(result_mul)



# 5. Write a class vector representing a vector of n dimensions. Overload the + and * operator which calculates the sum and the dot(.) product of them.



